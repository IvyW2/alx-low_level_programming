#include <stdio.h>

/**
 * main - prints all single digit numbers of base
 * 10 starting from 0
 * Return: Always 0 (Success)
 */
int main(void)
{
	int nt;

	for (nt = 0; nt < 10; nt++)
		printf("%d", nt);
	printf("\n");
	return (0);
}
